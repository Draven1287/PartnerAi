// Copy this to backend-config.js only after the production backend is deployed.
// Do not commit a real admin token here. The public website only needs the API URL.
window.LEARNING_AI_BACKEND_URL = 'https://api.learningai4you.com';
