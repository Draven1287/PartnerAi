services:
  learning-ai-minutes:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 8787
      DATA_FILE: /app/data/minutes.json
      DB_FILE: /app/data/learning-ai-store.json
      CORS_ORIGINS: ${CORS_ORIGINS:-https://learningai4you.com,https://www.learningai4you.com}
      ADMIN_TOKEN: ${ADMIN_TOKEN:?ADMIN_TOKEN is required}
    expose:
      - "8787"
    volumes:
      - learning-ai-minutes-data:/app/data
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8787/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped

volumes:
  learning-ai-minutes-data:
